// AUTO-GENERATED by src/codegen/transpile.ts — do not edit by hand.
// Deterministic replay of the 'universal-page-load' warm-up cache (no LLM at runtime).
// Regenerate with: npm run qa:transpile universal-page-load
import { test, expect } from '@playwright/test';

test.describe('generated: universal-page-load', () => {
  test('deterministic replay', async ({ page }) => {
    await page.goto("https://data.kyivcity.gov.ua", { waitUntil: 'domcontentloaded' });

    // ── Step 1: Wait until the page finishes loading
    await page.getByText("Verify you are human", { exact: false }).or(page.locator("xpath=/html/body/div[1]/div[1]/div[2]/div[1]/div[1]")).first().click();

    // ── Step 2: If a cookie consent or privacy banner is visible, accept or dismiss it using the safest available option
    await page.getByText("Відхилити", { exact: false }).or(page.locator("xpath=/html/body/div[3]/div[2]/div[1]/div[2]/button[normalize-space()=\"Відхилити\"]")).first().click();

    // ── Step 3: Scroll slightly down to reveal main page content if needed
    await page.mouse.wheel(0, 600);

    // Smoke check that the page is still alive after the replayed actions.
    await expect(page.locator('body')).toBeVisible();

    // Semantic assertions below are intentionally NOT auto-verified here; they
    // require the Midscene vision agent (run the scenario in regression mode).
    // SEMANTIC ASSERT (requires vision agent): The page has loaded and shows meaningful main content (not a blank page)
    // SEMANTIC ASSERT (requires vision agent): There is no obvious error message like 404, 500, or 'page not found'
    // SEMANTIC ASSERT (requires vision agent): The page has a visible title or main heading
  });
});
